# Ganamos API

Backend compartido de la landing y el panel.

- Local: `npm run dev` en `http://127.0.0.1:8787`
- Producción: Cloudflare Worker + D1

El Access Token de Meta va solo en `.dev.vars`, nunca en GitHub.

```
META_ACCESS_TOKEN=
PURCHASE_SEND_KEY=ganamos2026
META_TEST_EVENT_CODE=
```
